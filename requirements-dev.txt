# Python development tooling. Pinned exactly so CI, git hooks and local
# checkouts all run the same linter, and so Dependabot has a fixed starting
# point to raise upgrade pull requests against.
ruff==0.16.2
# pytest 8.3.x is the last line supporting Python 3.8, which this project still
# declares. Bump it in step with requires-python, not ahead of it.
pytest==9.1.1
